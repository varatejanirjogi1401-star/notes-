# Print ``Hello'' and ``World'' on the same line separated by a space without using + concatenation inside print(). (Hint: use sep or separate arguments.)
print("Hello", "World", sep=" ") # sep is used to specify the separator between the arguments in the print function. By default, it is a space, so we can simply use sep=" " to achieve the desired output.
